spring.application.name=notification-service

# Config Server Configuration
spring.config.import=optional:configserver:http://localhost:8081
spring.cloud.config.username=root
spring.cloud.config.password=s3cr3t

spring.boot.admin.client.url=http://localhost:8086

# Configuración del cliente Eureka
eureka.client.service-url.defaultZone=http://localhost:8099/eureka/
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true

# Spring DataSource Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/ayni_notifications?useSSL=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Spring Data JPA Configuration
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

spring.jmx.enabled=false

# Email Configuration - Gmail SMTP (CON TUS CREDENCIALES REALES)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=contact.pocketpartners@gmail.com
spring.mail.password=ymmb lnja wqrp upqq
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.debug=false
management.health.mail.enabled=false

# Configuración de remitente por defecto
email.from.name=Ayni Notifications
email.from.address=contact.pocketpartners@gmail.com

# Twilio Configuration - Usando variables de entorno (RECOMENDADO)
# Configurar en Windows: 
# set TWILIO_ACCOUNT_SID=tu_account_sid_real
# set TWILIO_AUTH_TOKEN=tu_auth_token_real
# set TWILIO_PHONE_NUMBER=tu_numero_twilio 

# SpringDoc Configuration
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true
springdoc.swagger-ui.path=/swagger-ui.html

# Management Configuration
management.health.circuitbreakers.enabled=true
management.endpoint.health.show-details=always
management.endpoints.web.exposure.include=*

server.port=8094
